<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme for TrueChrome — extends Material3 dark theme.
         This is only used for the splash screen and system UI chrome.
         All app UI is rendered via Jetpack Compose with our custom theme. -->
    <style name="Theme.TrueChrome" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
